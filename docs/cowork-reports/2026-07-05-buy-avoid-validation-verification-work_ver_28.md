# Codex Work Ver 28 - buy-avoid validation verification

## 1. 입력 리뷰

- 확인 파일: `docs/cowork-reports/2026-07-05-buy-avoid-validation-verification-review_ver_27.md`
- cowork 판정 요지:
  - `work_ver_27`의 P0/P1 보강은 수용 가능하다.
  - 2026-07-18 전까지는 P1인 전체 pytest 결과 보고 한 줄만 남긴다.
  - 2026-07-18 장후에는 §6의 P0 라운드로 E1 재측정과 E5 역발상 관찰을 함께 진행한다.
  - 105560 역방향 후보는 `p_down`과 `p_up`이 모두 양수 방향으로 보일 수 있으므로, 다음 재측정 때 `p_flat` IC와 `p_down`/`p_up` daily IC 관계를 같이 병기한다.

## 2. 이번 조치 범위

사용자 지시와 cowork 리뷰에 맞춰 이번 작업은 P1만 처리했다. 새 실험, 모델 재학습, gate 변경, active model 변경, 주문 정책 변경은 하지 않았다.

- P1: 전체 pytest 결과 보고 한 줄 추가.
- P0 대기: E1 재측정과 E5 역발상 관찰은 2026-07-18 이후 장후 라운드로 보류.

## 3. P1 결과

- 전체 pytest 결과: `python3 -m pytest -q` -> `444 passed, 59 subtests passed in 32.62s`.
- 이 결과는 `work_ver_27` 최종 검증에서 이미 실행된 결과이며, 이번 `work_ver_28`에서는 cowork가 지적한 보고 누락만 보완했다.

## 4. 2026-07-18 이후 진행 조건

2026-07-18은 토요일이다. 따라서 실제 P0 라운드는 2026-07-18 당일 장후가 아니라, 2026-07-18 이후 첫 거래일 장후에 진행하는 것이 안전하다.

다음 라운드에서 할 일:

1. E1 신호 IC 재측정 전체를 다시 실행한다.
2. 후보 3건의 같은 종목/같은 방향 재현성을 확인한다.
   - `005380 probability_up`
   - `035420 probability_down`
   - `105560 probability_down`
3. `105560`은 `probability_flat` IC와 `probability_down`/`probability_up` daily IC 관계를 함께 병기한다.
4. E5 역발상 관찰을 같은 라운드에서 실행한다.
5. threshold `0.40` excess sign을 재확인하되, 결과가 나오기 전까지 정책 반영이나 결론 확정 표현은 쓰지 않는다.

## 5. 자동화 반영

기존 장전/장후 heartbeat 자동화는 유지하되, 2026-07-18 이후 첫 거래일 장후에 위 P0 라운드를 한 번 수행하도록 조건을 덧붙였다. 별도 신규 자동화를 추가하지 않았다.

## 6. Codex 의견

이번 리뷰는 맞다. 지금은 추가 실험을 늘리기보다 이미 정한 관찰 기간까지 데이터가 쌓이게 두는 것이 더 안전하다. 특히 105560은 `probability_down`만 보면 역방향 후보처럼 보이지만, `probability_up`도 같이 양수로 움직인다면 모델이 방향성보다 종목 고유 변동성이나 특정 기간 효과를 잡고 있을 가능성이 있다. 그래서 다음 라운드에서 `p_flat`까지 같이 봐야 해석이 덜 흔들린다.

## 7. Self-review

- 누락한 작업: P1 보고 한 줄만 처리하라는 범위를 지켰다.
- 잘못 진행한 부분: 코드, 모델, gate, 주문 정책 변경 없음.
- 판단 위험: 07-18은 토요일이므로 첫 거래일 장후로 옮기는 판단을 명시했다.
- 검증: 문서 변경 후 `git diff --check` 대상이다.
- 다음 cowork 리뷰 필요 시점: 2026-07-18 이후 첫 거래일 장후 P0 라운드 결과가 나온 뒤.
